extends _template.pug

block konten
    .style-h1
        h1 News
    .covers
        .containeer
        each newsItem in news
            +newsItem(newsItem.imageSrc, newsItem.imageAlt, newsItem.description, newsItem.linkHref)