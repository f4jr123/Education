extends _template.pug

block konten
    .prestasi-h1
        h1 Prestasi
    .covers
        each item in prestasi
            +prestasiItem(item.dateDay, item.dateMonth, item.dateYear, item.title, item.images)